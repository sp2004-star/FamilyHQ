{
  "name": "familyhq",
  "version": "1.0.0",
  "description": "Family Document Vault - Secure document storage for families",
  "scripts": {
    "install:all": "cd backend && npm install --production && cd ../frontend && npm install --include=dev",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
